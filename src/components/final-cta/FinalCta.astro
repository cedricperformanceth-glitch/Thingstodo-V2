---
import { getFinalCtaRoutes } from '../../content/final-cta-routes';

interface Props {
  country: string;
}

const { country } = Astro.props;
const routes = getFinalCtaRoutes(country);
---

<section class="final-cta" aria-label="Make Your Own Atlas">
  <div class="final-cta__transition" aria-hidden="true"></div>

  <div class="final-cta__inner">
    <div class="final-cta__canvas">
      <svg class="final-cta__note final-cta__note--explore" viewBox="0 0 190 86" aria-hidden="true">
        <path class="final-cta__ink" d="M23 45C20 19 162 11 173 40C184 68 42 77 20 52C14 45 15 39 23 32" />
        <path class="final-cta__ink final-cta__ink--soft" d="M30 39C44 20 154 17 169 42C179 60 80 73 34 61" />
        <text x="95" y="52" text-anchor="middle">explore</text>
      </svg>

      <svg class="final-cta__note final-cta__note--yours" viewBox="0 0 220 82" aria-hidden="true">
        <text x="110" y="44" text-anchor="middle">make it yours</text>
        <path class="final-cta__ink" d="M40 57C78 62 129 61 178 55C190 54 198 55 202 57" />
        <path class="final-cta__ink final-cta__ink--soft" d="M47 62C89 65 145 63 187 58" />
      </svg>

      <img
        class="final-cta__logo"
        src="/assets/shared/make-your-own-atlas/Logo%20MY%20ATLAS%20CTA%20FINAL.webp"
        alt="Make Your Own Atlas"
        loading="lazy"
        decoding="async"
      />

      <div class="final-cta__actions" aria-label="Your Atlas and Atlas Routes">
        <a class="atlas-summary-link" href="/summary" aria-label="Build Your Atlas">
          <span>Build Your Atlas</span>
          <span class="atlas-summary-link__arrow" aria-hidden="true">↗</span>
        </a>

        {(routes[0] || routes[1]) && (
          <div class="atlas-route-selector" aria-label="Choose an Atlas route direction">
            {routes[0] ? (
              <a
                class="atlas-route atlas-route--first"
                href={routes[0].href}
                target="_blank"
                rel="noopener noreferrer"
                aria-label={`Open Atlas Route ${routes[0].label} in a new tab`}
              >
                <span class="atlas-route__label">{routes[0].label}</span>
                <span class="atlas-route__direction" aria-hidden="true">
                  <span class="atlas-route__line"></span>
                  <span class="atlas-route__arrow">↓</span>
                </span>
              </a>
            ) : <span aria-hidden="true"></span>}

            <div class="atlas-compass" aria-hidden="true">
              <img
                class="atlas-compass__asset"
                src="/assets/PDF/compass.webp"
                alt=""
                loading="lazy"
                decoding="async"
              />
            </div>

            {routes[1] ? (
              <a
                class="atlas-route atlas-route--second"
                href={routes[1].href}
                target="_blank"
                rel="noopener noreferrer"
                aria-label={`Open Atlas Route ${routes[1].label} in a new tab`}
              >
                <span class="atlas-route__label">{routes[1].label}</span>
                <span class="atlas-route__direction" aria-hidden="true">
                  <span class="atlas-route__arrow">↑</span>
                  <span class="atlas-route__line"></span>
                </span>
              </a>
            ) : <span aria-hidden="true"></span>}
          </div>
        )}
      </div>
    </div>
  </div>
</section>

<style>
  .final-cta {
    position: relative;
    isolation: isolate;
    width: 100vw;
    margin-left: calc(50% - 50vw);
    overflow: hidden;
    background: #f3ede0;
  }

  .final-cta__transition {
    height: clamp(2.5rem, 5vw, 4.5rem);
    background: linear-gradient(180deg, #1b2b24 0%, #263a31 20%, rgba(38, 58, 49, .54) 52%, rgba(243, 237, 224, .72) 82%, #f3ede0 100%);
  }

  .final-cta__inner {
    width: min(1180px, calc(100% - 2rem));
    margin: 0 auto;
  }

  .final-cta__canvas {
    position: relative;
    min-height: clamp(27.25rem, 38vw, 30rem);
    padding: clamp(2.75rem, 5vw, 4rem) 0 clamp(2.35rem, 3.8vw, 3rem);
  }

  .final-cta__logo {
    position: absolute;
    z-index: 2;
    left: 25%;
    top: 18%;
    display: block;
    width: min(35rem, 47vw);
    max-width: 50%;
    height: auto;
    transform: translateX(-50%);
  }

  .final-cta__note {
    position: absolute;
    z-index: 3;
    overflow: visible;
    pointer-events: none;
  }

  .final-cta__note text {
    fill: #315f83;
    font-family: 'Caveat', cursive;
    font-size: 31px;
    font-weight: 600;
    letter-spacing: .01em;
  }

  .final-cta__ink {
    fill: none;
    stroke: #315f83;
    stroke-width: 2.2;
    stroke-linecap: round;
    stroke-linejoin: round;
    opacity: .9;
  }

  .final-cta__ink--soft {
    stroke-width: 1.35;
    opacity: .55;
  }

  .final-cta__note--explore {
    top: 5.5%;
    left: 2.5%;
    width: clamp(8.1rem, 12.75vw, 10.6rem);
    transform: rotate(-10deg);
  }

  .final-cta__note--yours {
    top: 9%;
    left: 69%;
    width: clamp(9.45rem, 14.4vw, 11.7rem);
    transform: rotate(-8deg);
  }

  .final-cta__actions {
    position: absolute;
    z-index: 4;
    inset: 0;
    pointer-events: none;
  }

  .atlas-route,
  .atlas-summary-link {
    pointer-events: auto;
    text-decoration: none;
  }

  .atlas-summary-link {
    position: absolute;
    left: 25%;
    top: 57%;
    display: inline-flex;
    align-items: center;
    gap: 2rem;
    width: max-content;
    padding: .22rem .45rem .42rem .08rem;
    border-bottom: 1px solid currentColor;
    color: #315545;
    font-family: 'Segoe Print', 'Bradley Hand', cursive;
    font-size: 1.5rem;
    font-weight: 600;
    line-height: 1.05;
    text-align: left;
    white-space: nowrap;
    transform: translateX(-50%) rotate(-1deg);
    transition: transform 160ms ease, opacity 160ms ease;
  }

  .atlas-summary-link::before {
    content: '';
    position: absolute;
    right: 0;
    bottom: 0;
    height: .9rem;
    border-right: 1px solid currentColor;
  }

  .atlas-summary-link__arrow {
    display: inline-block;
    font-family: Georgia, 'Times New Roman', serif;
    font-size: 1.02rem;
    font-weight: 400;
    line-height: 1;
  }

  .atlas-summary-link:hover,
  .atlas-summary-link:focus-visible {
    opacity: .74;
    transform: translateX(-50%) rotate(0deg) translateY(-2px);
  }

  .atlas-summary-link:focus-visible,
  .atlas-route:focus-visible {
    outline: 2px solid rgba(49, 85, 69, .55);
    outline-offset: 5px;
    border-radius: 4px;
  }

  .atlas-route-selector {
    position: absolute;
    left: 75%;
    top: 36%;
    width: min(38rem, 52%);
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: center;
    gap: clamp(.8rem, 1.7vw, 1.7rem);
    transform: translateX(-50%);
    pointer-events: none;
  }

  .atlas-route {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: .45rem;
    min-width: 0;
    font-family: 'Caveat', cursive;
    font-weight: 650;
    line-height: 1;
    transition: transform 180ms ease, opacity 180ms ease;
  }

  .atlas-route--first {
    align-items: flex-end;
    color: #4f7180;
    text-align: right;
  }

  .atlas-route--second {
    align-items: flex-start;
    color: #9e5f53;
    text-align: left;
  }

  .atlas-route__label {
    display: block;
    font-size: clamp(1.25rem, 1.85vw, 1.7rem);
    white-space: nowrap;
  }

  .atlas-route__direction {
    display: flex;
    align-items: center;
    gap: .45rem;
    width: min(9rem, 100%);
    opacity: .62;
  }

  .atlas-route--first .atlas-route__direction {
    justify-content: flex-end;
  }

  .atlas-route--second .atlas-route__direction {
    justify-content: flex-start;
  }

  .atlas-route__line {
    display: block;
    width: clamp(2.3rem, 4.6vw, 4.8rem);
    height: 1px;
    background: currentColor;
    transform-origin: center;
    transition: transform 180ms ease, opacity 180ms ease;
  }

  .atlas-route__arrow {
    display: block;
    font-family: Georgia, 'Times New Roman', serif;
    font-size: 1.28rem;
    font-weight: 400;
    line-height: .8;
  }

  .atlas-route:hover {
    opacity: .76;
  }

  .atlas-route--first:hover {
    transform: translateX(-4px);
  }

  .atlas-route--second:hover {
    transform: translateX(4px);
  }

  .atlas-route:hover .atlas-route__line {
    transform: scaleX(1.12);
  }

  .atlas-compass {
    width: clamp(8rem, 11.5vw, 10rem);
    aspect-ratio: 1;
    pointer-events: none;
  }

  .atlas-compass__asset {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  @media (max-width: 760px) {
    .final-cta__canvas {
      min-height: 40rem;
      padding: 2.75rem 0 3rem;
    }

    .final-cta__logo {
      left: 50%;
      top: 10%;
      width: min(88vw, 36rem);
      max-width: 94%;
      transform: translateX(-50%);
    }

    .final-cta__note text {
      font-size: 28px;
    }

    .atlas-summary-link {
      left: 50%;
      top: 39%;
      font-size: 1.3rem;
    }

    .final-cta__note--yours {
      top: 54.5%;
      left: 50%;
      width: 9rem;
      transform: translateX(-50%) rotate(-8deg);
    }

    .final-cta__note--explore {
      top: 2.5%;
      left: 1.5%;
      width: 8.1rem;
      transform: rotate(-10deg);
    }

    .atlas-route-selector {
      left: 50%;
      top: 66%;
      width: 98%;
      gap: .8rem;
    }

    .atlas-route__label {
      font-size: clamp(1.12rem, 4.2vw, 1.35rem);
    }

    .atlas-route__direction {
      width: min(7.4rem, 100%);
    }

    .atlas-compass {
      width: clamp(6.7rem, 22vw, 7.8rem);
    }
  }

  @media (max-width: 520px) {
    .final-cta__canvas {
      min-height: 38rem;
      padding: 2.5rem 0 2.8rem;
    }

    .atlas-summary-link {
      top: 38%;
      font-size: 1.2rem;
    }

    .final-cta__note--yours {
      top: 53.5%;
      width: 8.5rem;
    }

    .final-cta__note--explore {
      top: 2%;
      width: 7.7rem;
    }

    .atlas-route-selector {
      grid-template-columns: 1fr 6.2rem 1fr;
      gap: .5rem;
      top: 65.5%;
    }

    .atlas-compass {
      width: 6.2rem;
    }

    .atlas-route__label {
      font-size: 1.06rem;
    }

    .atlas-route__direction {
      gap: .3rem;
    }

    .atlas-route__line {
      width: clamp(1.7rem, 8.5vw, 3rem);
    }
  }

  @media (max-width: 640px) {
    .final-cta__transition {
      height: 2.75rem;
    }

    .final-cta__inner {
      width: min(100% - 1.25rem, 1180px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .atlas-route,
    .atlas-route__line,
    .atlas-summary-link {
      transition: none;
    }
  }
</style>