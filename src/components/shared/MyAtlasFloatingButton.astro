<button
  class="my-atlas-fab"
  type="button"
  data-my-atlas-toggle
  aria-expanded="false"
  aria-controls="make-your-own-atlas-drawer"
  aria-label="Open Make Your Own Atlas"
>
  <span class="my-atlas-fab__count" data-my-atlas-fab-count aria-hidden="true">00</span>
  <span class="my-atlas-fab__art" aria-hidden="true">
    <img
      src="/assets/shared/make-your-own-atlas/Logo%20MY%20ATLAS.webp"
      alt=""
      width="2172"
      height="724"
      loading="eager"
      decoding="async"
    />
  </span>
</button>

<style>
  .my-atlas-fab {
    width: clamp(12.5rem, 14vw, 14.5rem);
    display: grid;
    grid-template-columns: minmax(0, 1fr) 2.2rem;
    align-items: center;
    column-gap: .45rem;
    box-sizing: border-box;
    padding: .2rem .25rem .2rem .1rem;
    overflow: hidden;
    border: 0;
    background: transparent;
    box-shadow: none;
    cursor: pointer;
  }

  .my-atlas-fab__count {
    grid-column: 2;
    grid-row: 1;
    width: 2rem;
    height: 2rem;
    display: grid;
    place-items: center;
    justify-self: center;
    box-sizing: border-box;
    border: 1px solid #b76645;
    border-radius: 50%;
    background: transparent;
    color: #b76645;
    font: 600 .7rem/1 Fraunces, serif;
  }

  .my-atlas-fab__art {
    grid-column: 1;
    grid-row: 1;
    min-width: 0;
    display: block;
  }

  .my-atlas-fab__art img {
    display: block;
    width: 100%;
    height: auto;
    background: transparent;
  }

  .my-atlas-fab:hover {
    filter: saturate(1.04);
  }

  .my-atlas-fab:focus-visible {
    outline: 2px solid #b76645;
    outline-offset: 2px;
  }

  @media (max-width: 700px) {
    .my-atlas-fab {
      width: 11rem;
      grid-template-columns: minmax(0, 1fr) 1.95rem;
      column-gap: .35rem;
      padding: .15rem .2rem .15rem .05rem;
    }

    .my-atlas-fab__count {
      width: 1.8rem;
      height: 1.8rem;
      font-size: .64rem;
    }
  }
</style>
