<button
  class="my-atlas-fab"
  type="button"
  data-my-atlas-toggle
  aria-expanded="false"
  aria-controls="make-your-own-atlas-drawer"
  aria-label="Open Make Your Own Atlas"
>
  <span class="my-atlas-fab__logo-slot" aria-hidden="true">
    <img
      class="my-atlas-fab__logo"
      src="/assets/shared/make-your-own-atlas/Logo%20MY%20ATLAS.webp"
      alt=""
      width="2172"
      height="724"
      loading="eager"
      decoding="async"
    />
  </span>

  <span class="my-atlas-fab__counter-slot" aria-hidden="true">
    <span class="my-atlas-fab__count" data-my-atlas-fab-count>00</span>
  </span>
</button>

<style>
  .my-atlas-fab {
    position: relative;
    width: 224px;
    height: 66px;
    display: grid;
    grid-template-columns: 75% 25%;
    align-items: center;
    box-sizing: border-box;
    margin: 0;
    padding: 6px 10px 6px 7px;
    overflow: visible;
    border: 1px solid rgba(49, 85, 69, .42);
    background:
      linear-gradient(rgba(247, 242, 230, .96), rgba(247, 242, 230, .96)),
      repeating-linear-gradient(0deg, transparent 0 8px, rgba(49, 85, 69, .035) 8px 9px);
    color: #315545;
    box-shadow: 4px 5px 0 rgba(49, 85, 69, .12), 0 12px 30px rgba(32, 60, 49, .12);
    transform: rotate(-.45deg);
    cursor: pointer;
    pointer-events: auto;
    transition: opacity 180ms ease, transform 180ms ease, box-shadow 180ms ease;
  }

  .my-atlas-fab::before {
    content: '';
    position: absolute;
    top: -5px;
    left: 31px;
    width: 42px;
    height: 10px;
    background: rgba(210, 177, 126, .66);
    transform: rotate(-3deg);
    pointer-events: none;
  }

  .my-atlas-fab__logo-slot,
  .my-atlas-fab__counter-slot {
    min-width: 0;
    min-height: 0;
    box-sizing: border-box;
  }

  .my-atlas-fab__logo-slot {
    height: 52px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    padding-right: .35rem;
  }

  .my-atlas-fab__logo {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    margin: 0;
  }

  .my-atlas-fab__counter-slot {
    height: 100%;
    display: grid;
    place-items: center;
  }

  .my-atlas-fab__count {
    min-width: 32px;
    width: 32px;
    height: 32px;
    display: grid;
    place-items: center;
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    border: 1px solid #b76645;
    border-radius: 50%;
    background: transparent;
    color: #b76645;
    font: 600 .72rem/1 Fraunces, serif;
    transform: rotate(4deg);
  }

  .my-atlas-fab:hover {
    transform: rotate(0) translateY(-2px);
    box-shadow: 5px 7px 0 rgba(49, 85, 69, .13), 0 16px 34px rgba(32, 60, 49, .15);
  }

  .my-atlas-fab:focus-visible {
    outline: 2px solid #b76645;
    outline-offset: 3px;
  }

  @media (max-width: 700px) {
    .my-atlas-fab {
      width: 202px;
      height: 60px;
    }

    .my-atlas-fab__logo-slot {
      height: 46px;
    }
  }

  @media (max-width: 380px) {
    .my-atlas-fab {
      width: 184px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .my-atlas-fab { transition: none; }
  }
</style>
