---
import type { AtlasEntity } from '../../core/models/types';
interface Props { entity: AtlasEntity; }
const { entity } = Astro.props;
---
<button class="button button--quiet" type="button" data-trip data-entity={JSON.stringify(entity)}>Add to My Atlas</button>
